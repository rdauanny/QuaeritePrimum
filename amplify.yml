version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 23.5.0
        - nvm use 23.5.0
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: /build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
